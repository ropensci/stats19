% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert.R
\name{stats19_to_parquet}
\alias{stats19_to_parquet}
\title{Convert raw STATS19 CSV files to Parquet}
\usage{
stats19_to_parquet(
  type = "collision",
  years = NULL,
  data_dir = get_data_directory(),
  output_dir = get_parquet_directory(),
  filename = NULL,
  partition_by = NULL,
  compression = "zstd",
  max_mem_gb = 4,
  temp_dir = tempdir(),
  overwrite = TRUE,
  silent = FALSE
)
}
\arguments{
\item{type}{Type of data to convert: \code{"collision"} (or \code{"accident"}),
\code{"casualty"}, \code{"vehicle"}, or \code{"all"}.}

\item{years}{Optional vector of years to convert. If \code{NULL} (default), all
available files for the requested type in \code{data_dir} are converted.}

\item{data_dir}{Directory containing raw downloaded CSV files.
Defaults to \code{get_data_directory()}.}

\item{output_dir}{Directory where the Parquet file(s) will be written.
Defaults to \code{get_parquet_directory()}.}

\item{filename}{Optional output filename. If \code{NULL} (default), the file is
named \verb{\{type\}s.parquet} (e.g. \code{collisions.parquet}).}

\item{partition_by}{Optional column name(s) to partition by, e.g. \code{"collision_year"}.
If supplied, a Hive-partitioned directory will be written.}

\item{compression}{Parquet compression codec: \code{"zstd"} (default) or \code{"snappy"}.}

\item{max_mem_gb}{Maximum memory limit in GB for DuckDB during conversion (default: 4).}

\item{temp_dir}{Path to DuckDB temporary spilling directory (default: \code{tempdir()}).}

\item{overwrite}{Logical. If \code{TRUE} (default), overwrites existing Parquet output.}

\item{silent}{Logical. Suppress informational messages? (default: \code{FALSE}).}
}
\value{
The path to the created Parquet file or directory (invisibly).
}
\description{
Reads raw STATS19 CSV files from the data directory and converts them into
highly compressed, typed, columnar Parquet files using DuckDB.
Normalises schema variations across historical releases (such as
\code{accident_index} vs \code{collision_index}, date string formats, and column renames).
}
\examples{
\donttest{
if (requireNamespace("duckdb", quietly = TRUE) &&
    requireNamespace("DBI", quietly = TRUE)) {
  # Convert collision files to Parquet in a temporary folder
  tmp_parquet <- file.path(tempdir(), "parquet")
  # stats19_to_parquet(type = "collision", output_dir = tmp_parquet)
}
}
}
