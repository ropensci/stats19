% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean.R
\name{junction_status}
\alias{junction_status}
\title{Classify collisions as at a junction or not}
\usage{
junction_status(
  junction_detail,
  junction_detail_historic = NULL,
  road_type = NULL
)
}
\arguments{
\item{junction_detail}{Vector of \code{junction_detail} values (numeric codes,
codes read as character, or formatted labels).}

\item{junction_detail_historic}{Vector of \code{junction_detail_historic}
values, the same length as \code{junction_detail}. Optional: if \code{NULL},
every row falls back to \code{junction_detail} and \code{road_type}.}

\item{road_type}{Vector of \code{road_type} values, the same length as
\code{junction_detail}. Optional: used to recover roundabouts and slip
roads hidden under \code{junction_detail} code 0 when historic is unknown.
Mini-roundabouts cannot be recovered this way, as \code{road_type} has no
separate mini-roundabout code.}
}
\value{
A character vector, the same length as \code{junction_detail}, of
\code{"junction"}, \code{"not junction"}, or \code{NA} where the status cannot be
determined.
}
\description{
From 2015 to 2023, \code{junction_detail} code 0 ("Not at junction or within
20 metres") also covers roundabouts, mini-roundabouts and slip roads, so
filtering on \code{junction_detail} alone puts junction collisions in the
non-junction group. \code{junction_status()} gives a clean junction/not
junction classification, using \code{junction_detail_historic} where it is
known (2023 and earlier, where it still separates these out) and falling
back to \code{junction_detail} together with \code{road_type} where it is not
(2024 onwards, where \code{junction_detail_historic} is unknown for every
row). See \url{https://github.com/ropensci/stats19/issues/328}.
}
\details{
Works on both raw codes (\code{get_stats19(format = FALSE)}) and formatted
labels (\code{get_stats19(format = TRUE)}, the default), since \code{format_stats19()}
keeps \code{junction_detail_historic} as its own column rather than merging it
into \code{junction_detail}. Also works when raw codes come through as
character rather than numeric, e.g. from an \code{all_varchar} CSV read.
}
\examples{
junction_status(
  junction_detail          = c(0, 0, 0, 13,  0,  0),
  junction_detail_historic = c(0, 1, 2, 3,  -1, -1),
  road_type                = c(6, 1, 6, 6,   1,  6)
)
}
