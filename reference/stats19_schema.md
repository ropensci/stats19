# Stats19 schema and variables

`stats19_schema` and `stats19_variables` contain metadata on stats19
data. `stats19_schema` is a look-up table matching codes provided in the
raw stats19 dataset with character strings.

## Note

The schema data can be (re-)generated using the script in the `data-raw`
directory.
